# Build

build bằng g++
trong terminal của BTL_DSA gõ: g++ -std=c++17 -Iinclude src/*.cpp src/app/*.cpp -o bloom
-> trong folder BTL_DSA xuất hiện file bloom.exe

# Chạy chương trình
chạy file bloom.exe chương trình sẽ được mở